# Phase 47 — Fix execute.md core bugs — Tasks

*Auto-extracted from a direct /rcode-add-phase bulk-fix request on 2026-08-09.*

Fix 4 GitHub issues in hanzlahabib/rcode, all touching execute.md and its close siblings.

**Issue 1014:** `rcode/workflows/execute.md`'s branch-protection pre-flight check refuses to run on branch `master` or `main` even when `branching_strategy: none` is configured in `.rcode/config.yaml` — read the actual pre-flight step (search execute.md for `branching_strategy` and for the branch check logic, likely near the top under a pre_flight or git-preflight section) and make it skip the check when `branching_strategy` is `none`. Separately in the same issue: execute.md tells the user to pass a flag called `--allow-main` to override the check, but `rcode/references/git-preflight.md` defines the actual flag as `--on-main` — pick ONE canonical name (recommend keeping whatever git-preflight.md's actual bash parsing logic checks for, since that is the real enforcement point) and fix execute.md's user-facing instruction text to match it exactly.

**Issue 1015** (the execute.md portion only — a sibling agent is handling the plan-research-validation.md portion of this same issue): execute.md around the `USE_WORKTREES` variable uses the pattern `$(node rcode-tools.cjs config-get workflow.use_worktrees || echo true)` — this never applies the `true` fallback because `config-get` exits 0 with empty stdout when the key is absent, so `||` never triggers. Fix by capturing into a variable first then using `:-` shell default expansion, exactly matching the working pattern plan.md already uses at its own lines 100, 246, and 488 (read those lines in plan.md first to copy the exact idiom).

**Issue 1017** (the execute.md portion only): execute.md documents parsing top-level JSON fields called `parallelization`, `branching_strategy`, `phase_found`, `phase_number`, `phase_name`, `phase_slug`, `plan_count`, `incomplete_count`, `roadmap_exists`, `phase_req_ids` from the `init execute` command output, but the real live JSON output only has `branching_strategy` nested under a `config` object, and the rest of those fields are simply absent from the real output — read the actual current output of running `node .rcode/bin/rcode-tools.cjs init execute` against any real phase number in this repo to see the true shape, then fix execute.md to read `branching_strategy` from `config.branching_strategy` instead of a bogus top-level field, and either find out where the other listed fields actually come from in the real tool output and correct the paths, or if they are genuinely nowhere in the output, note that clearly rather than silently leaving broken parsing instructions.

**Issue 1020:** `rcode/workflows/execute-sprint.md`, `execute.md`, and `rcode/workflows/plan-spawn-planner.md` all still treat a tag called `acceptance_criteria` as mandatory or as a real completion-precedence tier, but rcode-planner's actual followed contract (`rcode/references/planner-playbook.md` and `rcode/templates/sprint.md`, verified already) never emits that tag at all — it uses `done` and `evidence` instead, and the only programmatic check is `cmdPlanValidateEvidence` in rcode-tools.cjs which only checks for the `evidence` tag. Fix by removing `acceptance_criteria` as a mandatory/real tier from execute-sprint.md's completion-precedence section, from plan-spawn-planner.md's MANDATORY language and its quality-gate checklist item, and from execute.md wherever it appears, replacing references with the `done` and `evidence` tags that are actually enforced today — this matches what commit 2894765 already did on the producer side; this phase finishes the consumer side.

**Cross-cutting requirement for all 4 fixes:** also propagate matching changes into the `.rcode/` mirrored copies of these same files if they currently match the `rcode/` source (diff first). Commit each logical fix as its own commit referencing the relevant issue number (1014, 1015, 1017, 1020). Do not push.
