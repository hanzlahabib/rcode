# rcode — marketing kit

> Consulted via the `rcode-mariam` (Marketing & Growth) agent. **Every claim is grounded in `node benchmarks/facts.cjs` output (rcode v4.3.2) — no invented numbers, no fake stunts.**

## Verified facts (the only claims allowed)
- 497 automated tests, 100% passing, across 61 files
- 94,481 lines of portable methodology markdown
- 9,005-line tested CLI engine (1 runtime dependency: `ws`)
- 45 agents · 116 commands · 129 workflows · 88 skills
- Core ops resolve in ~33–38 ms locally with **0 LLM tokens**
- Install: `pnpm dlx @hanzlaa/rcode install`

## Taglines (each ≤10 words, fact it rests on)
1. **497 tests. Zero failing. Read the script.** ← 497 tests, 100% passing  ⭐ primary
2. Marketing rounds up. This repo ships the receipts. ← every figure reproducible
3. An engineering org in a folder. ← 45 agents / 116 commands / 129 workflows / 88 skills
4. One npm dependency. Ninety-four thousand lines of method. ← 1 dep; 94,481 lines
5. Your orchestration brain runs in 33ms, zero tokens. ← ~33ms, 0 LLM tokens
6. Methodology you can `grep`, not just believe. ← 94,481 lines on disk
7. 45 specialists. One install command. Any IDE. ← 45 agents; one-command install
8. No server, no vector DB, one dependency. ← 1 runtime dep; pure-stdlib dashboard

**Primary pick:** #1 — a falsifiable claim is the rarest thing in the AI-skills space, and "read the script" dares the reader to verify (which is the whole positioning, in seven words).

## Banner positioning (≤20 words)
> An engineering methodology shipped as plain markdown and one tested CLI — 497 passing tests, one dependency, runs in any IDE.

## Launch thread (grounded)
1. The AI-skills timeline is full of viral prompts with star counts and zero benchmarks. rcode is the opposite bet: a whole software-delivery methodology you can verify line by line. `pnpm dlx @hanzlaa/rcode install`
2. What's in the folder: 45 specialist agents, 116 commands, 129 workflows, 88 skills · 94,481 lines of portable markdown · a 9,005-line CLI engine with 1 runtime dependency. No multi-agent server. No vector DB. No lock-in.
3. The part nobody else shows: the proof. 497 automated tests, 0 failing. Intent routing + state checks resolve in ~33ms locally with 0 LLM tokens. Don't take it from me — run `node benchmarks/facts.cjs`.

## Hero-image concepts (generator-agnostic prompts)
See `hero-receipt.png` and `hero-org-in-folder.png` in this directory (generated via Codex `imagegen`).

- **A — "The receipt":** terminal printing the verified-facts table like a cash-register receipt; charcoal `#0E0E11`, single terminal-green accent, monospace, lots of negative space.
- **B — "Org in a folder":** a markdown file-tree that resolves into an org chart; light IDE aesthetic `#FAFAFA`, single blue accent `#2563EB`, flat vector.
- **C — "Rounds up vs. ships the script":** split panel — faded hype card with a big star badge (left) vs. a crisp passing test run `ok 497 — 0 failing` (right).
